#!/bin/bash

# This program converts the comics/mangas into a Kindle 
# supported format.

# This program must be run as a root. So obviously, 

# Of course, we can take 

DEFAULT="${PWD}"

"$1"

DEST=